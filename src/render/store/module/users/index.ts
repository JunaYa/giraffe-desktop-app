const state = {
  navList: [
    {
      id: 2001,
      name: 'CreditCard',
      icon: 'icon-in-box',
      path: '/users/person',
    },
  ],
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
}
